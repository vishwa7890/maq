# Email Configuration
EMAIL_PASSWORD=your_app_specific_password_here  # Generate this from your Google Account > Security > App passwords

# Google OAuth (Frontend + Backend)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_ID=your_google_client_id
# Comma-separated list. Leave empty to allow any domain.
ALLOWED_EMAIL_DOMAINS=gmail.com,yahoo.com

# Other environment variables if needed
